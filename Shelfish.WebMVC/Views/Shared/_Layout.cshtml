<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Shelfish</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <div class="navbar-brand">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Shelfish", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="navbar-nav">
                    <a class="nav-item">@Html.ActionLink("Home", "Index", "Home")</a>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a class="nav-item nav-link">@Html.ActionLink("Authors", "Index", "Author")</a>
                        <a class="nav-item nav-link">@Html.ActionLink("Books", "Index", "Book")</a>
                        <a class="nav-item nav-link">@Html.ActionLink("Audio", "Index", "Audiobook")</a>
                        <a class="nav-item nav-link">@Html.ActionLink("Shelves", "Index", "Bookshelf")</a>
                        <a class="nav-item nav-link">@Html.ActionLink("Reviews", "Index", "Review")</a>
                    }
                    <a class="nav-item nav-link">@Html.ActionLink("About", "About", "Home")</a>
                    <a class="nav-item nav-link">@Html.ActionLink("Contact", "Contact", "Home")</a>
                </div>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </nav>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Shelfish</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
